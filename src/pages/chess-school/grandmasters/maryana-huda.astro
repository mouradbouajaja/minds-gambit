---
import BaseLayout from '../../../layouts/BaseLayout.astro';

export const prerender = true;

const teacher = {
  name: "WGM Maryana Huda",
  shortTitle: "WGM",
  fideTitle: "Woman Grandmaster",
  trainerTitle: "FIDE Trainer (2022)",
  tagline: "Quality professional coaching for all levels, including for players with serious tournament goals.",
  photo: "https://image.lichess1.org/display?fmt=webp&h=350&op=thumbnail&path=morshynska1:coach:morshynska1:47WCJA8z.jpg&w=350&sig=7153456a989676933f39b96c09cc68502ebf226c",
  location: "Morshyn, Lviv region — Ukraine",
  fideRating: 2179,
  nationalRating: 2283,
  hourlyRate: "€20",
  packageRate: "€75 / 5 lessons",
  availability: "Accepting students",
  languages: ["English", "Spanish", "Ukrainian", "Russian", "Catalan"],
  email: "gudachess@gmail.com",
  lichessCoach: "https://lichess.org/coach/Morshynska1",
  lichessProfile: "https://lichess.org/@/Morshynska1",
  twitchProfile: "https://twitch.tv/maryanahuda"
};

const specialties = [
  "Openings — repertoire design, theory, your questions answered",
  "Middlegame strategy and positional understanding",
  "Endgame technique",
  "Tactics and calculation",
  "Advantage conversion and chess technique",
  "Game analysis with detailed human commentary",
  "Tournament preparation and opponent study",
  "Sports psychology and mental conditioning",
  "Prophylaxis and planning",
  "Work with chess classics and literature",
  "Spotting and neutralizing recurring mistakes",
  "Developing your own game style"
];

const freeStudies = [
  { title: "Lecture on the Center for beginners", url: "https://lichess.org/study/oPTsIN7c", chapters: 2 },
  { title: "Lecture on Piece Development for beginners", url: "https://lichess.org/study/b0ZqKFLd", chapters: 4 },
  { title: "Practice Exercises", url: "https://lichess.org/study/g7rXay67", chapters: 4 }
];
---

<BaseLayout title={`${teacher.name} — Chess Coach — Mind's Gambit Chess School`} description="Book lessons with WGM Maryana Huda — Woman Grandmaster, FIDE Trainer, and chess educator from Ukraine. Quality professional coaching for all levels, online.">
  <!-- Floating chess particle background -->
  <div class="particles-container" id="coach-particles"></div>
  <!-- Scroll progress ribbon -->
  <div class="scroll-progress" id="scrollProgress"></div>

  <nav>
  <div class="nav-inner">
    <a href="/" class="nav-logo"><img src="/images/logo.jpg" alt="Mind's Gambit" style="height:55px;width:55px;border-radius:50%;object-fit:cover;border:2px solid #c8a84e;margin-left:10px;vertical-align:middle;" /></a>
    <ul class="nav-links">
      <li><a href="/">Home</a></li>
      <li><a href="/#book">The Book</a></li>
      <li><a href="/watch">Documentary</a></li>
      <li><a href="/blog">Blog</a></li>
      <li><a href="/chess-school">Chess School</a></li>
      <li><a href="/#about">About</a></li>
      <li><a href="/#contact">Contact</a></li>
      <li><a href="https://buymeacoffee.com/mouradbouajaja" target="_blank" rel="noopener" class="nav-btn">&#9749; Buy Me a Coffee</a></li>
    </ul>
  </div>
</nav>

  <article class="coach-page">
    <a href="/chess-school/grandmasters" class="back-link" data-reveal>&larr; All Grandmasters</a>

    <!-- HERO -->
    <header class="coach-hero" data-reveal>
      <div class="hero-photo-wrap">
        <div class="photo-aura"></div>
        <div class="photo-ring"></div>
        <img src={teacher.photo} alt={teacher.name} class="hero-photo" loading="eager" referrerpolicy="no-referrer" />
        <span class="availability-badge"><span class="pulse-dot"></span>{teacher.availability}</span>
      </div>

      <div class="hero-content">
        <span class="eyebrow">Mind's Gambit Chess School &middot; Coach Profile</span>
        <h1 class="gradient-text">{teacher.name}</h1>
        <p class="tagline">{teacher.tagline}</p>

        <div class="title-row">
          <span class="title-chip primary">{teacher.shortTitle}</span>
          <span class="title-chip">{teacher.fideTitle}</span>
          <span class="title-chip">{teacher.trainerTitle}</span>
        </div>

        <div class="hero-cta">
          <a href={`mailto:${teacher.email}?subject=Chess%20Lessons%20with%20WGM%20Maryana%20Huda`} class="btn-primary cta-btn">
            <span class="btn-text">Book by Email</span>
            <span class="btn-shimmer"></span>
          </a>
          <a href={teacher.lichessCoach} target="_blank" rel="noopener" class="btn-secondary cta-btn">Book via Lichess &rarr;</a>
        </div>
      </div>
    </header>

    <!-- AT-A-GLANCE CARD -->
    <section class="at-a-glance" data-reveal>
      <div class="stat glass-card">
        <div class="stat-label">FIDE Rating</div>
        <div class="stat-value count-up" data-target="2179">0</div>
        <div class="stat-sub">Ukrainian: {teacher.nationalRating}</div>
      </div>
      <div class="stat glass-card">
        <div class="stat-label">Hourly Rate</div>
        <div class="stat-value">{teacher.hourlyRate}</div>
        <div class="stat-sub">{teacher.packageRate}</div>
      </div>
      <div class="stat glass-card">
        <div class="stat-label">Location</div>
        <div class="stat-value-sm">Ukraine 🇺🇦</div>
        <div class="stat-sub">Online lessons · worldwide</div>
      </div>
      <div class="stat glass-card">
        <div class="stat-label">Languages</div>
        <div class="stat-value count-up" data-target="5">0</div>
        <div class="stat-sub">{teacher.languages.join(" · ")}</div>
      </div>
    </section>

    <!-- BIO -->
    <section class="coach-section" data-reveal>
      <h2>About Maryana</h2>
      <p>Maryana Huda is a Woman Grandmaster (WGM) and certified FIDE Trainer from Morshyn in Ukraine's Lviv region. A holder of the Ukrainian Master of Sports title, she earned bronze medals at the 2015 Ukrainian Women's Championships in the U20 and rapid categories, and has been a winner and medalist in numerous national and international tournaments. She continues to compete actively in international events while maintaining deep ongoing study of chess theory, training methodology, and competitive practice.</p>
      <p>Her coaching experience spans nearly twenty years and began in the traditional system of specialized Children's and Youth Sports Schools (DYuSSh) in Ukraine, where long-term in-person development of tournament players formed the foundation of her teaching approach. Known for detailed and deeply explanatory game analysis, her coaching combines classical chess-school methods with modern understanding of sports psychology and competitive performance.</p>
      <p>She holds a Master's degree with honors in Physical Culture and Sports from Lviv State University of Physical Culture, where her research focused on psychological aspects of competitive chess, including concentration maintenance and advantage conversion under tournament pressure.</p>
    </section>

    <!-- PRICING -->
    <section class="pricing-section" data-reveal>
      <h2>Lessons &amp; Pricing</h2>
      <div class="pricing-grid">
        <div class="pricing-card tilt-card">
          <div class="pricing-eyebrow">Single Session</div>
          <div class="pricing-amount">€20<span class="pricing-unit">/ hour</span></div>
          <p class="pricing-description">A focused one-on-one session. Bring a recent game, a question, or a position you want to work through.</p>
          <a href={`mailto:${teacher.email}?subject=Single%20Lesson%20Inquiry%20%E2%80%94%20WGM%20Maryana%20Huda`} class="pricing-cta cta-btn">Book a Single Lesson</a>
        </div>

        <div class="pricing-card pricing-card-featured tilt-card">
          <div class="pricing-badge">Best Value</div>
          <div class="featured-glow"></div>
          <div class="pricing-eyebrow">Package</div>
          <div class="pricing-amount">€75<span class="pricing-unit">/ 5 lessons</span></div>
          <p class="pricing-description">Five hours of structured coaching at a reduced rate. The right choice for students serious about long-term improvement.</p>
          <a href={`mailto:${teacher.email}?subject=Five-Lesson%20Package%20Inquiry%20%E2%80%94%20WGM%20Maryana%20Huda`} class="pricing-cta pricing-cta-primary cta-btn">Book the 5-Lesson Package</a>
        </div>
      </div>
      <p class="pricing-note">Lessons conducted via Lichess, Microsoft Teams, Google Meet, Zoom, Telegram, or written analysis &mdash; whichever fits your workflow.</p>
    </section>

    <!-- SPECIALTIES -->
    <section class="coach-section" data-reveal>
      <h2>Coaching Specialties</h2>
      <p class="section-intro">Detailed, "human" analysis. Individual approach to every student. The full range of competitive chess preparation:</p>
      <ul class="specialty-grid">
        {specialties.map((item, i) => (
          <li class="specialty-item" style={`--delay: ${i * 60}ms`}><span class="specialty-mark">♞</span>{item}</li>
        ))}
      </ul>
    </section>

    <!-- TEACHING APPROACH -->
    <section class="coach-section" data-reveal>
      <h2>Teaching Approach</h2>
      <blockquote class="pull-quote">
        "I try to do the work for students as it's usually done for players who prepare to be professional ones &mdash; step by step. We can discuss your games and practice some important positions."
      </blockquote>
      <p>Lessons follow an individual curriculum built around your level, your goals, and your gaps. Common rhythm: analyze your own games, study targeted positions or themes, work on openings or endgames you want to sharpen, and discuss the psychological dimensions of tournament play.</p>
      <p>For tournament players: opening prep, opponent study, and the mental conditioning that separates strong club performance from elite results. For improvers: pattern recognition, calculation discipline, and the chess-school methods that build a real foundation.</p>
    </section>

    <!-- WHO -->
    <section class="coach-section" data-reveal>
      <h2>Who She Works Best With</h2>
      <blockquote class="pull-quote">
        "Motivated and serious learners interested in long-term improvement and deeper understanding of chess &mdash; especially tournament players and ambitious improvers."
      </blockquote>
      <p>Languages of instruction adapt to the student. Sessions available in English, Spanish, Ukrainian, Russian, or Catalan &mdash; whichever you're most comfortable with.</p>
    </section>

    <!-- FREE STUDIES -->
    <section class="coach-section" data-reveal>
      <h2>Sample Her Teaching &mdash; Free</h2>
      <p class="section-intro">Before booking, see her teaching style directly. Three free interactive Lichess studies authored by Maryana:</p>
      <div class="studies-grid">
        {freeStudies.map(study => (
          <a href={study.url} target="_blank" rel="noopener" class="study-card tilt-card">
            <div class="study-glow"></div>
            <span class="study-icon">♟</span>
            <h3 class="study-title">{study.title}</h3>
            <span class="study-meta">{study.chapters} chapters · Free on Lichess</span>
            <span class="study-cta">Open Study &rarr;</span>
          </a>
        ))}
      </div>
    </section>

    <!-- SCIENTIFIC -->
    <section class="coach-section" data-reveal>
      <h2>Scientific Work</h2>
      <p>Maryana's academic research examines the intersection of sports psychology and chess performance &mdash; how psychological factors, temperament, and stress management influence competitive outcomes. A selection of her published work:</p>
      <ul class="publications">
        <li><a href="https://journals.ldufk.lviv.ua/index.php/pahs/article/view/92" target="_blank" rel="noopener">Physical Activity, Health and Sport Journal · LDUFK</a></li>
        <li><a href="https://repository.ldufk.edu.ua/items/cb21e1d6-2112-469b-a3e3-99d1f3250158" target="_blank" rel="noopener">LDUFK Repository · Research paper (1)</a></li>
        <li><a href="https://repository.ldufk.edu.ua/items/f3338c64-10e3-4ec9-b765-85dd621ef320" target="_blank" rel="noopener">LDUFK Repository · Research paper (2)</a></li>
        <li><a href="https://ktmfks.pnu.edu.ua/wp-content/uploads/sites/99/2025/06/tezy_2025-2.pdf" target="_blank" rel="noopener">PNU Conference Proceedings (PDF)</a></li>
      </ul>
    </section>

    <!-- PROFILES -->
    <section class="coach-section" data-reveal>
      <h2>Find Maryana Online</h2>
      <div class="profile-links">
        <a href={teacher.lichessCoach} target="_blank" rel="noopener" class="profile-link tilt-card">
          <span class="profile-platform">Lichess Coach Page</span>
          <span class="profile-handle">@Morshynska1</span>
        </a>
                <a href={teacher.twitchProfile} target="_blank" rel="noopener" class="profile-link tilt-card">
          <span class="profile-platform">Twitch · occasional streams</span>
          <span class="profile-handle">@maryanahuda</span>
        </a>
      </div>
    </section>

    <!-- BIG CTA -->
    <section class="big-cta" data-reveal>
      <div class="big-cta-glow"></div>
      <div class="big-cta-inner">
        <h2>Ready to work with Maryana?</h2>
        <p>Reach out for an introductory session. Tell her your current level, your goals, and what you'd most like to improve. She'll design a plan from there.</p>
        <div class="big-cta-actions">
          <a href={`mailto:${teacher.email}?subject=Chess%20Lessons%20with%20WGM%20Maryana%20Huda`} class="btn-primary btn-large cta-btn">
            <span class="btn-text">Email Maryana Directly</span>
            <span class="btn-shimmer"></span>
          </a>
          <a href={teacher.lichessCoach} target="_blank" rel="noopener" class="btn-secondary btn-large cta-btn">Book on Lichess &rarr;</a>
        </div>
        <p class="cta-fineprint">Or reach the Mind's Gambit team at <a href="mailto:Mourad@mindsgambit.com">Mourad@mindsgambit.com</a> for an introduction.</p>
      </div>
    </section>

    <footer class="coach-footer">
      <p class="footer-credit">&copy; 2026 Mourad Bouajaja. Mind's Gambit Chess School is a production of &copy; MYB Studio LLC. Maryana Huda is featured here as an independent coach &mdash; all coaching arrangements are made directly with her.</p>
    </footer>
  </article>

  <script>
    // ===== FLOATING CHESS PARTICLES =====
    const particlesContainer = document.getElementById('coach-particles');
    const chessPieces = ['♔','♕','♖','♗','♘','♙','♚','♛','♜','♝','♞','♟'];
    if (particlesContainer) {
      for (let i = 0; i < 22; i++) {
        const p = document.createElement('span');
        p.className = 'particle';
        p.textContent = chessPieces[Math.floor(Math.random() * chessPieces.length)];
        p.style.left = Math.random() * 100 + '%';
        p.style.top = Math.random() * 100 + '%';
        p.style.animationDuration = (20 + Math.random() * 28) + 's';
        p.style.animationDelay = (-Math.random() * 25) + 's';
        p.style.fontSize = (1.3 + Math.random() * 2.4) + 'rem';
        p.style.opacity = (0.08 + Math.random() * 0.12).toFixed(2);
        particlesContainer.appendChild(p);
      }
    }

    // ===== SCROLL PROGRESS =====
    const bar = document.getElementById('scrollProgress');
    function updateBar() {
      if (!bar) return;
      const max = document.documentElement.scrollHeight - window.innerHeight;
      bar.style.width = ((window.scrollY / max) * 100) + '%';
    }
    window.addEventListener('scroll', updateBar, { passive: true });

    // ===== SCROLL REVEAL =====
    const revealObserver = new IntersectionObserver((entries) => {
      entries.forEach(entry => {
        if (entry.isIntersecting) {
          entry.target.classList.add('revealed');
          revealObserver.unobserve(entry.target);
        }
      });
    }, { threshold: 0.12, rootMargin: '0px 0px -60px 0px' });
    document.querySelectorAll('[data-reveal]').forEach(el => revealObserver.observe(el));

    // ===== COUNT-UP STATS =====
    const countObserver = new IntersectionObserver((entries) => {
      entries.forEach(entry => {
        if (entry.isIntersecting) {
          const el = entry.target;
          const target = parseInt(el.dataset.target, 10);
          const duration = 1400;
          const start = performance.now();
          function step(now) {
            const progress = Math.min((now - start) / duration, 1);
            const eased = 1 - Math.pow(1 - progress, 3);
            el.textContent = Math.floor(target * eased).toLocaleString();
            if (progress < 1) requestAnimationFrame(step);
            else el.textContent = target.toLocaleString();
          }
          requestAnimationFrame(step);
          countObserver.unobserve(el);
        }
      });
    }, { threshold: 0.5 });
    document.querySelectorAll('.count-up').forEach(el => countObserver.observe(el));

    // ===== 3D TILT ON CARDS =====
    document.querySelectorAll('.tilt-card').forEach(card => {
      card.addEventListener('mousemove', (e) => {
        const rect = card.getBoundingClientRect();
        const x = (e.clientX - rect.left) / rect.width - 0.5;
        const y = (e.clientY - rect.top) / rect.height - 0.5;
        card.style.transform = `perspective(900px) rotateY(${x * 6}deg) rotateX(${-y * 6}deg) translateY(-4px)`;
      });
      card.addEventListener('mouseleave', () => {
        card.style.transform = 'perspective(900px) rotateY(0) rotateX(0) translateY(0)';
      });
    });

    // ===== MAGNETIC CTAS =====
    document.querySelectorAll('.cta-btn').forEach(btn => {
      btn.addEventListener('mousemove', (e) => {
        const rect = btn.getBoundingClientRect();
        const x = e.clientX - rect.left - rect.width / 2;
        const y = e.clientY - rect.top - rect.height / 2;
        btn.style.transform = `translate(${x * 0.12}px, ${y * 0.18}px)`;
      });
      btn.addEventListener('mouseleave', () => {
        btn.style.transform = '';
      });
    });

    // ===== PARALLAX HERO PHOTO =====
    const heroPhoto = document.querySelector('.hero-photo-wrap');
    if (heroPhoto) {
      window.addEventListener('scroll', () => {
        if (window.scrollY < 800) {
          heroPhoto.style.transform = `translateY(${window.scrollY * 0.06}px)`;
        }
      }, { passive: true });
    }

    // ===== REDUCED MOTION =====
    if (window.matchMedia('(prefers-reduced-motion: reduce)').matches) {
      document.querySelectorAll('.particle').forEach(p => p.remove());
      document.querySelectorAll('[data-reveal]').forEach(el => el.classList.add('revealed'));
    }
  </script>
<!-- ============================================================
     MIND'S GAMBIT FAQ BOT — Static keyword-search bot.
     Free. No API calls. Edit the FAQ_DATA array below to add Q&As.
============================================================ -->
<button id="mg-faq-bubble" type="button" aria-label="Need help? Open Mind's Gambit Help" title="Click me for help &mdash; technical questions, engine issues, account, anything.">
  <span class="mg-faq-bubble-icon">&#9819;</span>
  <span class="mg-faq-bubble-label">Need help?</span>
  <span class="mg-faq-bubble-dot"></span>
</button>

<div id="mg-faq-panel" hidden>
  <header class="mg-faq-header">
    <div>
      <div class="mg-faq-title">Mind&rsquo;s Gambit Help</div>
      <div class="mg-faq-subtitle">Ask anything &middot; instant answers</div>
    </div>
    <button id="mg-faq-close" type="button" aria-label="Close">&times;</button>
  </header>
  <div class="mg-faq-messages" id="mg-faq-messages"></div>
  <form class="mg-faq-form" id="mg-faq-form" autocomplete="off">
    <input type="text" id="mg-faq-input" placeholder="Type your question&hellip;" maxlength="200" autocomplete="off" />
    <button type="submit" aria-label="Send">&#10142;</button>
  </form>
  <div class="mg-faq-suggested" id="mg-faq-suggested"></div>
</div>

<script is:inline>
(function() {
  'use strict';

  // ----- FAQ DATABASE -----
  // Edit this array to add/remove Q&As. Each entry has:
  //   keywords: array of words/phrases that trigger this answer (lowercase)
  //   q: short display question (shown in suggested list)
  //   a: the answer (HTML allowed)
  var FAQ_DATA = [
    { keywords: ['daily puzzle', 'puzzle of the day', 'today puzzle', 'how puzzle work'],
      q: 'How does the Daily Puzzle work?',
      a: 'Every visitor sees the <strong>same puzzle each day</strong>. You and a friend will see the same puzzle on the same day &mdash; perfect for friendly competition. Tomorrow it rotates automatically. We have a library of nearly 6 million puzzles, so you&rsquo;ll never run out.' },
    { keywords: ['account', 'login', 'sign up', 'signup', 'register'],
      q: 'Do I need an account?',
      a: '<strong>No.</strong> Mind&rsquo;s Gambit Chess School works without any signup. Your puzzle streak, rating, name, and stats are saved on this device so they&rsquo;re waiting for you next time you visit. Nothing to remember, nothing to log in to.' },
    { keywords: ['elo', 'rating', 'how rating work'],
      q: 'How does the rating work?',
      a: 'Your rating starts at <strong>1200</strong> and goes up when you win, down when you lose. Beating a stronger engine setting earns you a bigger jump. Losing to a weaker one costs you more points. We also remember your <strong>peak rating</strong> &mdash; the highest you&rsquo;ve ever reached. You never lose your peak, even if your current rating drops.' },
    { keywords: ['stockfish', 'engine', 'what engine'],
      q: 'What is Stockfish?',
      a: '<strong>Stockfish</strong> is the strongest chess engine in the world &mdash; the same engine grandmasters use to study their games. Mind&rsquo;s Gambit uses Stockfish everywhere: as your opponent on the Play page, and as the reviewer on the Analyze page. Completely free for you.' },
    { keywords: ['progress', 'save', 'data', 'sync', 'devices'],
      q: 'How is my progress saved?',
      a: 'Everything is saved <strong>on this device</strong>. No accounts. No tracking. The trade-off: if you switch to a different device or clear your browser data, your stats start fresh. That&rsquo;s the price of no-signup. We&rsquo;re building optional cross-device sync as a future feature for anyone who wants it.' },
    { keywords: ['coach', 'lesson', 'book', 'teacher', 'grandmaster'],
      q: 'Can I book a lesson with a coach?',
      a: 'Yes. Click <a href="/chess-school/grandmasters">Grandmasters</a> in the nav (or the tile on the Chess School hub). Each coach has their own profile with hourly rate, languages, and a contact button. Currently featuring <strong>WGM Maryana Huda</strong> with more titled players joining soon.' },
    { keywords: ['become coach', 'apply', 'titled', 'join roster'],
      q: 'How do I become a coach on Mind\'s Gambit?',
      a: 'If you’re a titled player (GM, IM, WGM, WIM, FM, WFM, NM, CM), visit the <a href="/chess-school/grandmasters">Grandmasters page</a> and click <strong>"Apply as a Founding Coach"</strong>. <strong>Free for 6 months</strong> — you keep 100% of your lesson fees, no exclusivity required.' },
    { keywords: ['free', 'cost', 'price', 'how much'],
      q: 'Is Mind\'s Gambit free?',
      a: 'All the puzzle, engine, and analysis features are <strong>completely free</strong>. No ads, no subscriptions, no upsells. Only paid feature: one-on-one lessons with our titled coaches, which goes directly to the coach.' },
    { keywords: ['tactics trainer', 'tactics by theme', 'pin fork', 'training'],
      q: 'How does the Tactics Trainer work?',
      a: 'Pick a tactical theme &mdash; pin, fork, mate-in-2, endgame, and many more &mdash; then practice puzzles with that pattern. <strong>73 themes</strong> to choose from, across nearly 6 million puzzles. Your accuracy is tracked while you train.' },
    { keywords: ['train now', 'random puzzle', 'just train'],
      q: 'What is Train Now?',
      a: '<strong>Train Now</strong> is the fastest way to start practicing: one click, random puzzle loads, solve it, next puzzle auto-loads. No theme filter, no menus. Tracks your solved / attempted / accuracy stats per session.' },
    { keywords: ['analysis', 'analyze game', 'pgn', 'review game'],
      q: 'How does Game Analysis work?',
      a: 'Paste your game on the <a href="/chess-school/analyze">Analyze page</a>. Your game will be reviewed by the <strong>latest and strongest engine on the planet, Stockfish 17</strong> &mdash; the Mind&rsquo;s Gambit preferred engine. You&rsquo;ll see a clear breakdown of every move: <strong>Blunder</strong>, <strong>Mistake</strong>, <strong>Inaccuracy</strong>, or <strong>Best Move</strong> &mdash; plus the better move you could have played. Everything in plain English you can follow, whether you&rsquo;re 8 or 80.' },
    { keywords: ['pgn', 'pgn format', 'where to get pgn', 'export game', 'download game'],
      q: 'Where do I get my game file (PGN)?',
      a: 'Every chess platform lets you export your game as a small text file called a PGN. Look for an <strong>Export</strong>, <strong>Download</strong>, or <strong>Share</strong> option in your game view &mdash; it&rsquo;s usually one click. For games played over the board, you can type the moves into any analysis tool. PGN is just a simple format that lists every move &mdash; like a written transcript of the game.' },
    { keywords: ['streak', 'daily streak', 'streak count'],
      q: 'How does the daily streak work?',
      a: 'Solve the Daily Puzzle on consecutive days to build a streak. Miss a day and the streak resets to 1 (not zero &mdash; not a punishment, just an honest signal). Your streak is saved on this device.' },
    { keywords: ['play another person', 'pass and play', 'two player', 'with friend'],
      q: 'Can I play vs another person?',
      a: '<strong>Coming soon.</strong> Pass &amp; Play (two players, one device) is on the roadmap. Online matchmaking against other Mind’s Gambit users is a longer-term build. For now, the Play page is vs the engine.' },
    { keywords: ['mobile', 'phone', 'iphone', 'android'],
      q: 'Does it work on mobile?',
      a: 'Yes. All pages are responsive. The board is drag-and-drop on touch. Daily Puzzle, Train Now, Tactics, Play, and Analysis all work on phone. If anything looks broken on your device, please email mourad@mindsgambit.com.' },
    { keywords: ['browser', 'safari', 'chrome', 'firefox'],
      q: 'Which browser should I use?',
      a: 'Any modern browser: Chrome, Edge, Firefox, Safari. Older browsers may feel slower because the chess engine runs right on your device.' },
    { keywords: ['stockfish 17', 'latest stockfish', 'engine version', 'how strong engine'],
      q: 'How strong is the engine?',
      a: 'On the <strong>Play page</strong>, the engine is rated around 3300 &mdash; strong enough to beat almost any human player. On the <strong>Analyze page</strong>, we use <strong>Stockfish 17</strong>, the latest and strongest version, around 3700 strength &mdash; far above any human grandmaster. You get top-tier analysis for free.' },
    { keywords: ['compare', 'different from', 'other sites', 'why mind', 'why choose'],
      q: 'What makes Mind\'s Gambit different?',
      a: 'Three things: (1) <strong>no ads, no upsells, no tracking</strong> &mdash; we have nothing to sell you; (2) <strong>connected to The Mind&rsquo;s Gambit book and documentary</strong>, so chess sits alongside writing about chess; (3) we feature a <strong>small, hand-picked roster of titled coaches</strong> &mdash; not thousands of unverified profiles. We&rsquo;re building a focused, intellectual home for chess.' },
    { keywords: ['founder', 'who built', 'mourad', 'mourad bouajaja'],
      q: 'Who built this?',
      a: '<a href="/#about">Mourad Bouajaja</a> — author of <em>The Mind’s Gambit: Chess, Madness, and the Battle for the Mind</em>, documentary maker, chess player. Mind’s Gambit Chess School is a production of MYB Studio LLC.' },
    { keywords: ['book', 'the book', 'where to buy'],
      q: 'Where can I read The Mind\'s Gambit book?',
      a: 'Visit <a href="/#book">The Book section</a> on the homepage for sample chapters and purchase links. The chess school is named after the book; the book is built on the documentary; everything connects.' },
    { keywords: ['documentary', 'watch', 'episodes', 'video'],
      q: 'How do I watch the documentary?',
      a: 'Go to <a href="/watch">/watch</a> (Documentary). The Mind’s Gambit is a 10-episode documentary series chronicling the intellectual journey behind the book. Episodes are also available on YouTube.' },
    { keywords: ['donate', 'support', 'buy coffee'],
      q: 'How can I support Mind\'s Gambit?',
      a: 'If any of this has helped you, you can <a href="https://buymeacoffee.com/mouradbouajaja" target="_blank" rel="noopener">buy us a coffee</a>. There’s also a tip jar in the top nav of every page (the gold button). Beyond money: tell a chess friend about us. Word of mouth is the only marketing we do.' },
    { keywords: ['bug', 'report', 'error', 'broken', 'not working'],
      q: 'Something is broken — how do I report it?',
      a: 'Email <a href="mailto:mourad@mindsgambit.com">mourad@mindsgambit.com</a> with: (1) which page, (2) what device/browser, (3) what you expected, (4) what happened. Screenshots help a lot. We fix things fast.' },
    { keywords: ['engine stall', 'engine stuck', 'engine not moving', 'engine freeze', 'stockfish stuck', 'stockfish not moving', 'engine slow', 'no engine move', 'engine took'],
      q: 'The engine is stalling or not making a move',
      a: 'Try these in order: (1) <strong>refresh the page</strong> &mdash; this fixes most stalls; (2) <strong>lower the engine strength</strong> &mdash; on the highest levels, the engine can take 10-20 seconds to think; (3) try a modern browser (Chrome, Edge, Firefox, Safari). If it keeps happening, email <a href="mailto:mourad@mindsgambit.com">mourad@mindsgambit.com</a> with what device and browser you&rsquo;re using and we&rsquo;ll figure it out.' },
    { keywords: ['board not loading', 'board blank', 'board missing', 'pieces missing', 'board white', 'board empty'],
      q: 'The chess board is not loading',
      a: 'Try: (1) <strong>hard refresh</strong> (Ctrl+Shift+R on Windows, Cmd+Shift+R on Mac); (2) <strong>turn off any ad-blocker</strong> on this site &mdash; some block the chess board files; (3) check your browser settings allow scripts. If the board still doesn&rsquo;t load, email <a href="mailto:mourad@mindsgambit.com">mourad@mindsgambit.com</a> with a screenshot.' },
    { keywords: ['puzzle stuck', 'puzzle not solving', 'puzzle frozen', 'cant solve', 'wrong answer'],
      q: 'The puzzle is stuck or my answer was marked wrong',
      a: 'Each puzzle has <strong>one specific solution</strong>. Even if your move looks good, only the intended sequence counts as solved &mdash; this is how tactical training works. Click <strong>Hint</strong> or <strong>Show Solution</strong> to see the answer. If you think a puzzle is genuinely broken, email <a href="mailto:mourad@mindsgambit.com">mourad@mindsgambit.com</a> with the puzzle ID and we&rsquo;ll review it.' },
    { keywords: ['lost progress', 'rating reset', 'stats gone', 'lost streak'],
      q: 'I lost my rating / streak / stats',
      a: 'Your progress is stored <strong>on this device only</strong>. It resets if you: (1) clear your browser data, (2) switch to a different browser or device, (3) open the site in private/incognito mode. We&rsquo;re building optional cross-device sync as a future feature. For now, stick to the same browser on the same device to keep your progress.' },
    { keywords: ['credits', 'open source', 'attribution', 'license', 'legal', 'powered by', 'technical', 'what powers', 'under the hood'],
      q: 'Credits & legal &mdash; what powers Mind\'s Gambit?',
      a: 'Mind&rsquo;s Gambit is built on the world&rsquo;s top open-source chess engine, <strong>Stockfish</strong> (free under the GPL v3 license). Our <strong>Mind&rsquo;s Gambit Training Boards</strong> use trusted open-source libraries for piece movement and rendering. Puzzles are sourced from public-domain chess databases. Optional coach commentary on the Analyze page is powered by <strong>Anthropic Claude</strong>. Everything is free for you to use here. Full attribution and license notices appear in the page footers as required by the underlying licenses.' },
    { keywords: ['contact', 'email', 'reach out'],
      q: 'How do I contact Mourad?',
      a: 'Email <a href="mailto:mourad@mindsgambit.com">mourad@mindsgambit.com</a>. Or use the <a href="/#contact">Contact section</a> on the homepage.' },
    { keywords: ['privacy', 'tracking', 'cookies', 'data'],
      q: 'Does Mind\'s Gambit track me?',
      a: '<strong>No tracking, no analytics, no ads.</strong> Your puzzle progress and rating live on <em>your</em> device &mdash; we never see them. When you use Game Analysis, the chess position is sent to the engine so it can compute the review &mdash; nothing about you personally is sent or stored.' }
  ];

  // ----- Bot logic -----
  var bubble = document.getElementById('mg-faq-bubble');
  var panel = document.getElementById('mg-faq-panel');
  var closeBtn = document.getElementById('mg-faq-close');
  var form = document.getElementById('mg-faq-form');
  var input = document.getElementById('mg-faq-input');
  var messagesEl = document.getElementById('mg-faq-messages');
  var suggestedEl = document.getElementById('mg-faq-suggested');

  if (!bubble || !panel) return;

  function openPanel() {
    panel.removeAttribute('hidden');
    panel.classList.add('mg-faq-open');
    input.focus();
    if (!messagesEl.children.length) greet();
  }
  function closePanel() {
    panel.setAttribute('hidden', '');
    panel.classList.remove('mg-faq-open');
  }
  bubble.addEventListener('click', function() {
    if (panel.hasAttribute('hidden')) openPanel(); else closePanel();
  });
  closeBtn.addEventListener('click', closePanel);

  function addMessage(text, sender) {
    var div = document.createElement('div');
    div.className = 'mg-faq-msg mg-faq-msg-' + sender;
    div.innerHTML = text;
    messagesEl.appendChild(div);
    messagesEl.scrollTop = messagesEl.scrollHeight;
  }

  function score(query, keywords) {
    var q = query.toLowerCase();
    var s = 0;
    for (var i = 0; i < keywords.length; i++) {
      var k = keywords[i].toLowerCase();
      if (q.indexOf(k) !== -1) s += k.length * 2;
      // Also score individual words
      var words = k.split(/\s+/);
      for (var w = 0; w < words.length; w++) {
        if (words[w].length >= 3 && q.indexOf(words[w]) !== -1) s += 1;
      }
    }
    return s;
  }

  function answer(query) {
    var best = null;
    var bestScore = 0;
    for (var i = 0; i < FAQ_DATA.length; i++) {
      var s = score(query, FAQ_DATA[i].keywords);
      if (s > bestScore) { bestScore = s; best = FAQ_DATA[i]; }
    }
    if (best && bestScore >= 3) return best.a;
    return null;
  }

  function greet() {
    addMessage('Hi &mdash; welcome to <strong>Mind&rsquo;s Gambit Help</strong>. I&rsquo;m here for <em>anything</em> you run into on the site:<br><br>&bull; <strong>Technical issues</strong> &mdash; engine stalling, board not loading, puzzle stuck, page broken<br>&bull; <strong>How things work</strong> &mdash; Daily Puzzle, ELO rating, Train Now, Tactics, Analysis<br>&bull; <strong>Account &amp; data</strong> &mdash; progress saving, switching devices, privacy<br>&bull; <strong>Coaches &amp; lessons</strong> &mdash; booking a titled coach, becoming a coach<br>&bull; <strong>Anything else</strong> &mdash; the book, documentary, contact Mourad<br><br>Type your question below, or pick one of these to get started:', 'bot');
    showSuggestions(['Engine is stalling / not moving', 'How does the Daily Puzzle work?', 'How does the ELO rating work?', 'Can I book a lesson with a coach?', 'Is Mind&rsquo;s Gambit free?', 'Something is broken &mdash; how do I report it?']);
  }

  function showSuggestions(questions) {
    suggestedEl.innerHTML = '';
    for (var i = 0; i < questions.length; i++) {
      var btn = document.createElement('button');
      btn.type = 'button';
      btn.className = 'mg-faq-suggest';
      btn.textContent = questions[i];
      (function(q) { btn.addEventListener('click', function() { handleQuery(q); }); })(questions[i]);
      suggestedEl.appendChild(btn);
    }
  }

  function handleQuery(query) {
    suggestedEl.innerHTML = '';
    addMessage(query, 'user');
    var a = answer(query);
    if (a) {
      setTimeout(function() { addMessage(a, 'bot'); }, 200);
    } else {
      setTimeout(function() {
        addMessage('I don’t have a stock answer for that one yet. Email <a href="mailto:mourad@mindsgambit.com">mourad@mindsgambit.com</a> and Mourad will reply personally — he usually answers within a day.', 'bot');
        // Show 4 random other suggestions
        var sample = [];
        var pool = FAQ_DATA.slice();
        for (var i = 0; i < 4 && pool.length; i++) {
          var idx = Math.floor(Math.random() * pool.length);
          sample.push(pool[idx].q);
          pool.splice(idx, 1);
        }
        showSuggestions(sample);
      }, 200);
    }
  }

  form.addEventListener('submit', function(e) {
    e.preventDefault();
    var q = input.value.trim();
    if (!q) return;
    input.value = '';
    handleQuery(q);
  });
})();
</script>

<style>
#mg-faq-bubble { position: fixed; bottom: 20px; right: 20px; width: 86px; min-height: 88px; padding: 10px 8px 8px; border-radius: 18px; background: linear-gradient(135deg, #c9a84c, #d4b556); border: 0; cursor: pointer; box-shadow: 0 8px 22px rgba(201,168,76,0.45), 0 4px 10px rgba(0,0,0,0.3); display: flex; flex-direction: column; align-items: center; justify-content: center; gap: 4px; z-index: 10000; transition: transform 0.2s, box-shadow 0.2s; font-family: 'EB Garamond', Georgia, serif; }
#mg-faq-bubble:hover { transform: translateY(-2px); box-shadow: 0 12px 28px rgba(201,168,76,0.6), 0 6px 14px rgba(0,0,0,0.35); }
.mg-faq-bubble-icon { font-size: 32px; color: #0f111c; line-height: 1; }
.mg-faq-bubble-label { font-size: 0.78rem; font-weight: 700; color: #0f111c; letter-spacing: 0.02em; line-height: 1; white-space: nowrap; }
.mg-faq-bubble-dot { position: absolute; top: 6px; right: 6px; width: 10px; height: 10px; background: #ef4444; border-radius: 50%; border: 2px solid #d4b556; }
#mg-faq-panel { position: fixed; bottom: 92px; right: 20px; width: 360px; max-width: calc(100vw - 40px); height: 540px; max-height: calc(100vh - 120px); background: #0f111c; border: 1px solid rgba(201,168,76,0.4); border-radius: 16px; display: flex; flex-direction: column; box-shadow: 0 18px 50px rgba(0,0,0,0.55); z-index: 10000; font-family: 'EB Garamond', Georgia, serif; overflow: hidden; }
#mg-faq-panel[hidden] { display: none !important; }
.mg-faq-header { padding: 14px 18px; background: rgba(201,168,76,0.12); border-bottom: 1px solid rgba(201,168,76,0.25); display: flex; justify-content: space-between; align-items: center; flex-shrink: 0; }
.mg-faq-title { font-family: 'Playfair Display', Georgia, serif; color: #c9a84c; font-size: 1rem; font-weight: 700; letter-spacing: 0.05em; }
.mg-faq-subtitle { color: rgba(232,224,208,0.55); font-size: 0.78rem; font-style: italic; margin-top: 2px; }
#mg-faq-close { background: transparent; border: 0; color: rgba(232,224,208,0.7); font-size: 26px; line-height: 1; cursor: pointer; padding: 0 4px; }
#mg-faq-close:hover { color: #c9a84c; }
.mg-faq-messages { flex: 1 1 auto; min-height: 0; padding: 16px 14px; overflow-y: auto; overflow-x: hidden; display: flex; flex-direction: column; gap: 10px; background: rgba(15,17,28,0.95); scrollbar-width: thin; scrollbar-color: rgba(201,168,76,0.4) transparent; }
.mg-faq-messages::-webkit-scrollbar { width: 8px; }
.mg-faq-messages::-webkit-scrollbar-track { background: transparent; }
.mg-faq-messages::-webkit-scrollbar-thumb { background: rgba(201,168,76,0.35); border-radius: 4px; }
.mg-faq-messages::-webkit-scrollbar-thumb:hover { background: rgba(201,168,76,0.6); }
.mg-faq-msg { padding: 10px 14px; border-radius: 12px; font-size: 0.92rem; line-height: 1.45; max-width: 88%; word-wrap: break-word; }
.mg-faq-msg-bot { background: rgba(201,168,76,0.1); border: 1px solid rgba(201,168,76,0.22); color: rgba(232,224,208,0.92); align-self: flex-start; }
.mg-faq-msg-user { background: #c9a84c; color: #0f111c; align-self: flex-end; }
.mg-faq-msg-bot a { color: #c9a84c; text-decoration: underline; }
.mg-faq-msg-bot strong { color: #c9a84c; }
.mg-faq-msg-bot em { color: rgba(232,224,208,0.65); font-style: italic; }
.mg-faq-form { display: flex; gap: 8px; padding: 12px 14px; border-top: 1px solid rgba(201,168,76,0.2); background: rgba(15,17,28,0.95); flex-shrink: 0; }
#mg-faq-input { flex: 1; background: rgba(30,34,52,0.55); border: 1px solid rgba(201,168,76,0.25); border-radius: 8px; padding: 9px 12px; color: #e8e0d0; font-family: 'EB Garamond', Georgia, serif; font-size: 0.95rem; box-sizing: border-box; }
#mg-faq-input:focus { outline: none; border-color: #c9a84c; }
.mg-faq-form button[type="submit"] { background: #c9a84c; color: #0f111c; border: 0; width: 40px; height: 40px; border-radius: 8px; font-size: 18px; cursor: pointer; font-weight: 700; }
.mg-faq-form button[type="submit"]:hover { background: #d4b556; }
.mg-faq-suggested { padding: 0 14px 12px; display: flex; flex-wrap: wrap; gap: 6px; background: rgba(15,17,28,0.95); flex-shrink: 0; max-height: 96px; overflow-y: auto; }
.mg-faq-suggest { background: rgba(30,34,52,0.6); border: 1px solid rgba(201,168,76,0.25); color: #c9a84c; padding: 6px 10px; border-radius: 99px; font-size: 0.78rem; font-family: 'EB Garamond', Georgia, serif; cursor: pointer; transition: all 0.15s; }
.mg-faq-suggest:hover { background: rgba(201,168,76,0.15); border-color: #c9a84c; }
@media (max-width: 480px) {
  #mg-faq-panel { width: calc(100vw - 24px); right: 12px; bottom: 108px; height: calc(100vh - 140px); }
  #mg-faq-bubble { right: 14px; bottom: 14px; width: 74px; min-height: 78px; }
  .mg-faq-bubble-icon { font-size: 28px; }
  .mg-faq-bubble-label { font-size: 0.7rem; }
}
</style>

</BaseLayout>

<style is:global>
nav { background: rgba(15,17,28,0.92); border-bottom: 1px solid rgba(201,168,76,0.18); position: sticky; top: 0; z-index: 100; backdrop-filter: blur(14px); -webkit-backdrop-filter: blur(14px); }
.nav-inner { max-width: 1200px; margin: 0 auto; padding: 0.75rem 2rem; display: flex; align-items: center; justify-content: space-between; }
.nav-links { display: flex; gap: 2rem; list-style: none; margin: 0; padding: 0; align-items: center; }
.nav-links a { color: #e8e0d0; text-decoration: none; font-size: 0.9rem; letter-spacing: 0.1em; text-transform: uppercase; font-family: 'Playfair Display', Georgia, serif; transition: color 0.3s; }
.nav-links a:hover { color: #c9a84c; }
.nav-btn { background: #c9a84c !important; color: #0f111c !important; padding: 0.5rem 1.25rem; border-radius: 6px; font-weight: 700; transition: transform 0.2s, box-shadow 0.2s; }
.nav-btn:hover { transform: translateY(-1px); box-shadow: 0 6px 20px rgba(201,168,76,0.35); }

/* ====== PARTICLES ====== */
.particles-container { position: fixed; inset: 0; pointer-events: none; overflow: hidden; z-index: 0; }
.particle { position: absolute; color: #c9a84c; will-change: transform; animation: float linear infinite; }
@keyframes float {
  0% { transform: translate(0, 0) rotate(0deg); }
  50% { transform: translate(35px, -50px) rotate(180deg); }
  100% { transform: translate(0, -100vh) rotate(360deg); }
}

/* ====== SCROLL PROGRESS ====== */
.scroll-progress { position: fixed; top: 0; left: 0; height: 3px; width: 0%; background: linear-gradient(90deg, #c9a84c, #f4d989, #c9a84c); z-index: 200; transition: width 0.1s ease-out; box-shadow: 0 0 18px rgba(201,168,76,0.6); }

/* ====== PAGE ====== */
.coach-page { max-width: 1100px; margin: 0 auto; padding: 2.5rem 1.5rem 4rem; font-family: 'EB Garamond', Georgia, serif; color: #e0e0e0; position: relative; z-index: 1; }
.back-link { color: #c9a84c; text-decoration: none; font-size: 0.9rem; letter-spacing: 0.05em; display: inline-block; margin-bottom: 2rem; font-family: 'Playfair Display', Georgia, serif; transition: opacity 0.25s; }
.back-link:hover { opacity: 0.75; transform: translateX(-3px); }

/* ====== REVEAL ====== */
[data-reveal] { opacity: 0; transform: translateY(28px); transition: opacity 0.85s cubic-bezier(.2,.7,.2,1), transform 0.85s cubic-bezier(.2,.7,.2,1); }
[data-reveal].revealed { opacity: 1; transform: translateY(0); }

/* ====== HERO ====== */
.coach-hero { display: grid; grid-template-columns: 340px 1fr; gap: 3.5rem; align-items: center; margin-bottom: 4rem; padding-bottom: 3.5rem; border-bottom: 1px solid rgba(201,168,76,0.2); }

.hero-photo-wrap { width: 340px; position: relative; will-change: transform; }
.photo-aura {
  position: absolute; inset: -30px; border-radius: 24px;
  background: radial-gradient(circle, rgba(201,168,76,0.35) 0%, transparent 65%);
  filter: blur(28px); animation: pulseAura 5s ease-in-out infinite;
  z-index: 0;
}
@keyframes pulseAura {
  0%, 100% { opacity: 0.55; transform: scale(1); }
  50% { opacity: 0.85; transform: scale(1.07); }
}
.photo-ring {
  position: absolute; inset: -6px; border-radius: 20px;
  background: conic-gradient(from 0deg, #c9a84c, #f4d989, #c9a84c, #b88a30, #c9a84c);
  animation: rotateRing 12s linear infinite;
  z-index: 1;
}
@keyframes rotateRing { to { transform: rotate(360deg); } }
.hero-photo {
  position: relative; width: 340px; height: 340px; object-fit: cover; border-radius: 16px;
  border: 4px solid #0f111c; box-shadow: 0 20px 60px rgba(0,0,0,0.55);
  z-index: 2; display: block;
}
.availability-badge {
  position: absolute; bottom: 18px; left: 18px;
  background: rgba(20,32,22,0.92); color: #4ade80;
  padding: 0.5rem 0.95rem; border-radius: 999px;
  font-size: 0.72rem; font-weight: 700; letter-spacing: 0.08em;
  text-transform: uppercase; backdrop-filter: blur(8px);
  border: 1px solid rgba(74,222,128,0.4); z-index: 3;
  display: flex; align-items: center; gap: 0.5rem;
  font-family: 'Playfair Display', Georgia, serif;
}
.pulse-dot {
  width: 8px; height: 8px; border-radius: 50%; background: #4ade80;
  box-shadow: 0 0 0 0 rgba(74,222,128,0.7); animation: pulseDot 2s infinite;
}
@keyframes pulseDot {
  0% { box-shadow: 0 0 0 0 rgba(74,222,128,0.7); }
  70% { box-shadow: 0 0 0 12px rgba(74,222,128,0); }
  100% { box-shadow: 0 0 0 0 rgba(74,222,128,0); }
}

.hero-content { display: flex; flex-direction: column; gap: 1rem; }
.eyebrow { color: #c9a84c; font-size: 0.8rem; text-transform: uppercase; letter-spacing: 0.22em; }
.coach-hero h1 { font-family: 'Playfair Display', Georgia, serif; font-size: 3.2rem; line-height: 1.05; margin: 0; }
.gradient-text {
  background: linear-gradient(120deg, #f4d989 0%, #c9a84c 40%, #e8e0d0 70%, #c9a84c 100%);
  background-size: 200% 100%;
  -webkit-background-clip: text; background-clip: text;
  -webkit-text-fill-color: transparent; color: transparent;
  animation: shimmer 6s ease-in-out infinite;
}
@keyframes shimmer { 0%, 100% { background-position: 0% 50%; } 50% { background-position: 100% 50%; } }
.tagline { font-size: 1.15rem; color: rgba(232,224,208,0.8); line-height: 1.55; font-style: italic; margin: 0; }

.title-row { display: flex; flex-wrap: wrap; gap: 0.5rem; }
.title-chip {
  background: rgba(201,168,76,0.13); color: #c9a84c;
  padding: 0.45rem 1rem; border-radius: 999px;
  font-size: 0.8rem; letter-spacing: 0.04em;
  font-family: 'Playfair Display', Georgia, serif;
  border: 1px solid rgba(201,168,76,0.22);
  transition: transform 0.2s, background 0.2s;
}
.title-chip:hover { transform: translateY(-2px); background: rgba(201,168,76,0.2); }
.title-chip.primary { background: linear-gradient(135deg, #c9a84c, #d4b356); color: #0f111c; font-weight: 800; border: none; box-shadow: 0 4px 14px rgba(201,168,76,0.35); }

.hero-cta { display: flex; gap: 1rem; margin-top: 0.5rem; flex-wrap: wrap; }
.btn-primary {
  position: relative; display: inline-flex; align-items: center; gap: 0.5rem;
  background: linear-gradient(135deg, #c9a84c, #d4b356);
  color: #0f111c; padding: 1rem 1.85rem; border-radius: 10px;
  text-decoration: none; font-weight: 700; font-size: 1rem;
  letter-spacing: 0.04em; transition: all 0.25s;
  box-shadow: 0 6px 22px rgba(201,168,76,0.32);
  font-family: 'Playfair Display', Georgia, serif; overflow: hidden;
}
.btn-primary:hover { box-shadow: 0 10px 32px rgba(201,168,76,0.55); }
.btn-text { position: relative; z-index: 1; }
.btn-shimmer {
  position: absolute; top: 0; left: -120%; width: 60%; height: 100%;
  background: linear-gradient(120deg, transparent 0%, rgba(255,255,255,0.45) 50%, transparent 100%);
  transform: skewX(-20deg);
}
.btn-primary:hover .btn-shimmer { animation: shineSweep 0.9s ease forwards; }
@keyframes shineSweep { to { left: 120%; } }

.btn-secondary {
  display: inline-block; background: transparent; color: #c9a84c;
  padding: 1rem 1.85rem; border-radius: 10px; text-decoration: none;
  font-weight: 600; font-size: 1rem; letter-spacing: 0.04em;
  border: 1px solid #c9a84c; transition: all 0.25s;
  font-family: 'Playfair Display', Georgia, serif;
  backdrop-filter: blur(6px);
}
.btn-secondary:hover { background: rgba(201,168,76,0.12); }
.btn-large { padding: 1.2rem 2.4rem; font-size: 1.1rem; }

/* ====== AT A GLANCE ====== */
.at-a-glance {
  display: grid; grid-template-columns: repeat(4, 1fr); gap: 1.25rem;
  margin-bottom: 4rem;
}
.glass-card {
  background: rgba(30,34,52,0.45);
  backdrop-filter: blur(12px); -webkit-backdrop-filter: blur(12px);
  border: 1px solid rgba(201,168,76,0.22);
  border-radius: 16px;
  padding: 1.75rem 1.25rem;
  text-align: center;
  position: relative; overflow: hidden;
  transition: transform 0.3s, border-color 0.3s, box-shadow 0.3s;
}
.glass-card::before {
  content: ''; position: absolute; top: 0; left: 0; right: 0; height: 1px;
  background: linear-gradient(90deg, transparent, rgba(201,168,76,0.7), transparent);
}
.glass-card:hover { transform: translateY(-4px); border-color: rgba(201,168,76,0.45); box-shadow: 0 14px 36px rgba(0,0,0,0.35); }
.stat-label { color: rgba(232,224,208,0.55); font-size: 0.72rem; text-transform: uppercase; letter-spacing: 0.18em; margin-bottom: 0.65rem; font-family: 'Playfair Display', Georgia, serif; }
.stat-value { font-family: 'Playfair Display', Georgia, serif; font-size: 2.5rem; color: #c9a84c; line-height: 1; margin-bottom: 0.5rem; font-weight: 700; }
.stat-value-sm { font-family: 'Playfair Display', Georgia, serif; font-size: 1.4rem; color: #e8e0d0; line-height: 1.2; margin-bottom: 0.5rem; }
.stat-sub { color: rgba(232,224,208,0.6); font-size: 0.8rem; line-height: 1.4; }

/* ====== SECTIONS ====== */
.coach-section { margin-bottom: 4rem; }
.coach-section h2 {
  font-family: 'Playfair Display', Georgia, serif; font-size: 2rem; color: #c9a84c;
  margin: 0 0 1.5rem; position: relative; padding-bottom: 0.75rem;
}
.coach-section h2::after {
  content: ''; position: absolute; left: 0; bottom: 0; width: 56px; height: 2px;
  background: linear-gradient(90deg, #c9a84c, transparent);
}
.coach-section p { font-size: 1.1rem; line-height: 1.75; margin: 0 0 1rem; }
.coach-section p:last-child { margin-bottom: 0; }
.section-intro { color: rgba(232,224,208,0.8); font-size: 1.05rem; margin-bottom: 1.75rem !important; }

.pull-quote {
  font-family: 'Playfair Display', Georgia, serif;
  font-size: 1.35rem; font-style: italic; color: #e8e0d0;
  line-height: 1.55; margin: 0 0 1.5rem;
  padding: 1.5rem 2rem; border-left: 3px solid #c9a84c;
  background: linear-gradient(90deg, rgba(201,168,76,0.08), transparent);
  border-radius: 0 8px 8px 0;
}

/* ====== PRICING ====== */
.pricing-section {
  margin-bottom: 4rem; padding: 3rem 2.5rem;
  background: linear-gradient(135deg, rgba(201,168,76,0.08) 0%, rgba(30,34,52,0.55) 100%);
  border: 1px solid rgba(201,168,76,0.25); border-radius: 20px;
  position: relative; overflow: hidden;
}
.pricing-section::before {
  content: ''; position: absolute; top: -50%; left: -50%; width: 200%; height: 200%;
  background: radial-gradient(circle, rgba(201,168,76,0.06) 0%, transparent 50%);
  animation: rotateRing 25s linear infinite; pointer-events: none;
}
.pricing-section > * { position: relative; }
.pricing-section h2 { font-family: 'Playfair Display', Georgia, serif; font-size: 2rem; color: #c9a84c; margin: 0 0 2rem; text-align: center; padding-bottom: 0; }
.pricing-section h2::after { display: none; }
.pricing-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 1.5rem; }
.pricing-card {
  background: rgba(15,17,28,0.72); border: 1px solid rgba(201,168,76,0.22);
  border-radius: 14px; padding: 2.25rem; position: relative;
  transition: transform 0.3s, box-shadow 0.3s, border-color 0.3s;
  cursor: default;
}
.pricing-card:hover { border-color: rgba(201,168,76,0.5); box-shadow: 0 14px 40px rgba(0,0,0,0.4); }
.pricing-card-featured { border-color: #c9a84c; box-shadow: 0 14px 40px rgba(201,168,76,0.18); }
.featured-glow {
  position: absolute; inset: -1px; border-radius: 14px; pointer-events: none;
  background: linear-gradient(135deg, rgba(201,168,76,0.4), transparent 40%, transparent 60%, rgba(201,168,76,0.4));
  -webkit-mask: linear-gradient(#fff 0 0) content-box, linear-gradient(#fff 0 0);
  -webkit-mask-composite: xor; mask-composite: exclude;
  padding: 1px; opacity: 0; transition: opacity 0.3s;
}
.pricing-card-featured:hover .featured-glow { opacity: 1; }
.pricing-badge {
  position: absolute; top: -14px; right: 22px;
  background: linear-gradient(135deg, #c9a84c, #f4d989);
  color: #0f111c; padding: 0.35rem 0.95rem; border-radius: 999px;
  font-size: 0.7rem; font-weight: 800; letter-spacing: 0.1em;
  text-transform: uppercase; font-family: 'Playfair Display', Georgia, serif;
  box-shadow: 0 4px 14px rgba(201,168,76,0.4);
}
.pricing-eyebrow { color: #c9a84c; font-size: 0.75rem; text-transform: uppercase; letter-spacing: 0.2em; margin-bottom: 0.65rem; font-family: 'Playfair Display', Georgia, serif; }
.pricing-amount { font-family: 'Playfair Display', Georgia, serif; font-size: 3.2rem; color: #e8e0d0; font-weight: 700; line-height: 1; margin-bottom: 1rem; }
.pricing-unit { font-size: 1.1rem; color: rgba(232,224,208,0.55); font-weight: 400; margin-left: 0.4rem; }
.pricing-description { color: rgba(232,224,208,0.8); font-size: 1rem; line-height: 1.55; margin: 0 0 1.5rem; }
.pricing-cta {
  display: inline-block; padding: 0.85rem 1.5rem; background: transparent;
  border: 1px solid #c9a84c; color: #c9a84c; text-decoration: none;
  border-radius: 8px; font-family: 'Playfair Display', Georgia, serif;
  font-size: 0.95rem; letter-spacing: 0.04em; transition: all 0.25s;
}
.pricing-cta:hover { background: rgba(201,168,76,0.12); }
.pricing-cta-primary { background: linear-gradient(135deg, #c9a84c, #d4b356); color: #0f111c; font-weight: 700; border: none; box-shadow: 0 5px 18px rgba(201,168,76,0.32); }
.pricing-cta-primary:hover { box-shadow: 0 8px 24px rgba(201,168,76,0.5); }
.pricing-note { color: rgba(232,224,208,0.6); font-size: 0.95rem; line-height: 1.55; text-align: center; margin-top: 1.75rem; font-style: italic; }

/* ====== SPECIALTIES ====== */
.specialty-grid { list-style: none; padding: 0; margin: 0; display: grid; grid-template-columns: repeat(auto-fit, minmax(280px, 1fr)); gap: 0.4rem 1.5rem; }
.specialty-item {
  display: flex; align-items: flex-start; gap: 0.7rem;
  color: #e0e0e0; font-size: 1rem; line-height: 1.55;
  padding: 0.6rem 0; border-bottom: 1px solid rgba(201,168,76,0.08);
  opacity: 0; transform: translateX(-12px);
  animation: slideIn 0.6s cubic-bezier(.2,.7,.2,1) forwards;
  animation-delay: var(--delay, 0ms);
}
[data-reveal] .specialty-item { animation-play-state: paused; }
[data-reveal].revealed .specialty-item { animation-play-state: running; }
@keyframes slideIn { to { opacity: 1; transform: translateX(0); } }
.specialty-mark { color: #c9a84c; font-size: 1.05rem; flex-shrink: 0; transition: transform 0.25s; }
.specialty-item:hover .specialty-mark { transform: scale(1.25) rotate(-8deg); }

/* ====== STUDIES ====== */
.studies-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(260px, 1fr)); gap: 1.25rem; }
.study-card {
  position: relative; overflow: hidden;
  display: flex; flex-direction: column; gap: 0.55rem;
  padding: 1.75rem 1.5rem; background: rgba(30,34,52,0.55);
  border: 1px solid rgba(201,168,76,0.22); border-radius: 14px;
  text-decoration: none; color: inherit; transition: all 0.3s;
  cursor: pointer;
}
.study-card:hover { border-color: #c9a84c; }
.study-glow {
  position: absolute; top: -50%; right: -50%; width: 200px; height: 200px;
  background: radial-gradient(circle, rgba(201,168,76,0.25), transparent 70%);
  filter: blur(40px); opacity: 0; transition: opacity 0.4s;
}
.study-card:hover .study-glow { opacity: 1; }
.study-icon { font-size: 2.2rem; color: #c9a84c; line-height: 1; }
.study-title { font-family: 'Playfair Display', Georgia, serif; font-size: 1.15rem; color: #e8e0d0; margin: 0.25rem 0; line-height: 1.35; }
.study-meta { color: rgba(232,224,208,0.55); font-size: 0.85rem; }
.study-cta { color: #c9a84c; font-family: 'Playfair Display', Georgia, serif; font-size: 0.9rem; margin-top: 0.5rem; transition: transform 0.25s; }
.study-card:hover .study-cta { transform: translateX(4px); }

/* ====== PUBLICATIONS ====== */
.publications { list-style: none; padding: 0; margin: 1rem 0 0; }
.publications li { padding: 0.75rem 0; border-bottom: 1px solid rgba(201,168,76,0.1); }
.publications a { color: #c9a84c; text-decoration: none; font-size: 1rem; transition: opacity 0.2s; }
.publications a:hover { text-decoration: underline; opacity: 0.85; }

/* ====== PROFILES ====== */
.profile-links { display: grid; grid-template-columns: repeat(auto-fit, minmax(220px, 1fr)); gap: 1rem; }
.profile-link {
  display: flex; flex-direction: column; gap: 0.3rem;
  padding: 1.25rem 1.35rem; background: rgba(30,34,52,0.55);
  border: 1px solid rgba(201,168,76,0.22); border-radius: 12px;
  text-decoration: none; color: inherit;
  transition: all 0.25s; cursor: pointer;
}
.profile-link:hover { border-color: #c9a84c; }
.profile-platform { color: #e8e0d0; font-size: 0.95rem; font-family: 'Playfair Display', Georgia, serif; }
.profile-handle { color: #c9a84c; font-size: 0.88rem; font-family: 'EB Garamond', Georgia, serif; }

/* ====== BIG CTA ====== */
.big-cta {
  position: relative; overflow: hidden;
  margin: 5rem 0 2rem; padding: 4rem 2rem;
  background: linear-gradient(135deg, rgba(201,168,76,0.15) 0%, rgba(30,34,52,0.65) 100%);
  border: 1px solid rgba(201,168,76,0.4); border-radius: 20px;
  text-align: center;
}
.big-cta-glow {
  position: absolute; top: 50%; left: 50%; transform: translate(-50%, -50%);
  width: 70%; height: 130%;
  background: radial-gradient(ellipse, rgba(201,168,76,0.18), transparent 60%);
  filter: blur(40px); pointer-events: none;
}
.big-cta-inner { max-width: 640px; margin: 0 auto; position: relative; }
.big-cta h2 { font-family: 'Playfair Display', Georgia, serif; font-size: 2.4rem; color: #e8e0d0; margin: 0 0 1rem; padding-bottom: 0; }
.big-cta h2::after { display: none; }
.big-cta p { color: rgba(232,224,208,0.85); font-size: 1.15rem; line-height: 1.6; margin: 0 0 2rem; }
.big-cta-actions { display: flex; gap: 1rem; justify-content: center; flex-wrap: wrap; }
.cta-fineprint { color: rgba(232,224,208,0.6) !important; font-size: 0.92rem !important; margin-top: 2rem; }
.cta-fineprint a { color: #c9a84c; text-decoration: underline; }

/* ====== FOOTER ====== */
.coach-footer { margin-top: 3rem; padding-top: 2rem; border-top: 1px solid rgba(201,168,76,0.12); text-align: center; }
.footer-credit { color: rgba(232,224,208,0.45); font-size: 0.8rem; line-height: 1.55; max-width: 720px; margin: 0 auto; }

/* ====== RESPONSIVE ====== */
@media (max-width: 880px) {
  .coach-hero { grid-template-columns: 1fr; text-align: center; gap: 2.5rem; }
  .hero-photo-wrap { margin: 0 auto; width: 260px; }
  .hero-photo { width: 260px; height: 260px; }
  .coach-hero h1 { font-size: 2.4rem; }
  .title-row, .hero-cta { justify-content: center; }
  .at-a-glance { grid-template-columns: repeat(2, 1fr); }
  .pricing-grid { grid-template-columns: 1fr; }
  .pricing-section { padding: 2rem 1.5rem; }
  .big-cta { padding: 3rem 1.5rem; }
  .big-cta h2 { font-size: 1.85rem; }
  .pull-quote { font-size: 1.15rem; padding: 1.25rem 1.5rem; }
}
</style>
